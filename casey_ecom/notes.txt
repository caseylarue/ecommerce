Altered/ Created: cart, cart_products, orders, customers (added cart_type)

Pay Button  >>

Sweep all Cart Info to Orders Table
- query to get cart info
cart_id, product_id, product_qty


- query to push cart info to orders table
need to input customer info to generate customer id before order table can be populated.

Gather Shipping Information

Gather Billing Information


Insert into customers 
(first_name, last_name, address, address_2, city, state, zipcode, card_type, credit_card_number, security_code, expiration_date, created_at)
VALUES
(?,?,?,?,?,?,?,?,?,?,?,?,NOW())

$result["billing_first_name"]
$result["billing_last_name"]
$result["billing_address"]
$result["billing_address_2"]
$result["billing_city"]
$result["billing_state"]
$result["billing_zip"]
$result["billing_card_type"]
$result["billing_card_number"]
$result["billing_security_code"]
$result["billing_expiration"]